import type { Source } from '@/lib/types';
export const sampleBodies = [
{title:'README excerpt',type:'md',rawText:`# BobCo Service README\nThe service exposes a REST API for onboarding widgets. Developers should start by reading the architecture overview and then run the local setup.\n\n## Repository layout\nRoutes live in src/routes, domain logic in src/domain, and shared adapters in src/adapters. Keep route handlers thin and move business rules into domain services.\n\n## First PR expectation\nA first pull request should add a small route or fix a documented bug, include tests, and update docs when behavior changes.`},
{title:'Local setup instructions',type:'md',rawText:`# Local Setup\nInstall Node 22 or newer and pnpm 9. Run pnpm install from the repository root. Copy .env.example to .env.local and set DATABASE_URL. Start dependencies with docker compose up -d postgres redis. Run pnpm dev to start the API on port 3000.\n\n## Validation\nRun pnpm typecheck and pnpm test before opening a pull request.`},
{title:'Testing guide',type:'md',rawText:`# Testing Guide\nUnit tests use Vitest and should sit beside the code as *.test.ts files. Integration tests live in tests/integration and require the docker compose services. Prefer testing public behavior over private implementation details.\n\n## Debugging failures\nIf integration tests cannot connect, confirm postgres is healthy, DATABASE_URL matches .env.local, and migrations have run with pnpm db:migrate.`},
{title:'Architecture overview',type:'md',rawText:`# Architecture Overview\nThe API follows a route-domain-adapter structure. Routes validate requests and call domain services. Domain services contain business decisions and do not import framework request objects. Adapters isolate external systems such as Postgres, Redis, and third-party APIs.\n\n## Reliability\nBackground jobs must be idempotent. Use job IDs for deduplication and log structured fields for customerId, jobId, and requestId.`},
{title:'Troubleshooting notes',type:'md',rawText:`# Troubleshooting Notes\nWhen the dev server fails after dependency changes, remove .next and restart pnpm dev. For database errors, check that docker compose ps shows postgres healthy. For Redis connection refused, run docker compose up -d redis. If TypeScript path aliases fail, restart the TypeScript server.`}
] as const;
export function makeSampleSources(projectId:string): Source[]{return sampleBodies.map((s,i)=>({id:`sample_${i+1}`,projectId,title:s.title,type:s.type,rawText:s.rawText,createdAt:new Date().toISOString(),metadata:{label:'Sample source material only'}}));}
